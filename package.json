{
  "name": "wayfarer",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "compile-data": "node scripts/compile-data.js",
    "dev": "node scripts/compile-data.js && vite",
    "dev:designer": "node scripts/compile-data.js && vite --config vite.designer.config.js",
    "dev:editor": "node scripts/compile-data.js && vite --config vite.editor.config.js",
    "build": "node scripts/compile-data.js && vite build",
    "preview": "vite preview",
    "lint": "eslint js/",
    "lint:fix": "eslint js/ --fix",
    "check": "tsc --project jsconfig.json",
    "validate": "node scripts/compile-data.js && eslint js/ && tsc --project jsconfig.json"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "eslint": "^10.0.3",
    "globals": "^17.4.0",
    "typescript": "^5.9.3",
    "vite": "^6.0.0",
    "wrangler": "^4.71.0"
  }
}
